# 2026-09-16

## IK 反解 limit 参数诊断（sim2sim 工作空间扫描）

### 做了什么

新建三个诊断工具，全部走 `fkref.py`（独立参考实现）+ `robot.yaml` 真值，无硬编码：

| 工具 | 作用 |
|---|---|
| `core/tools/ws_scan_mearm_v1.py` | J1/J2/J3 网格扫描 → TCP 实际 XYZ 范围 |
| `core/tools/ik_limit_diag.py` | 几何量求导 + 端点量化 + 舵机换算 |
| `core/tools/ik_reflexivity_diag.py` | FK→IK 自反性：FK 产出的点能否解得回原角 |
| `core/tools/ik_limit_gap.py` | 差距对照表（汇总输出） |

⚠️ 环境：`simulation/mujoco/` 下 `fkref.py` 依赖 numpy；已在
`C:\Users\lx176\.workbuddy\binaries\python\envs\default` 装 numpy 2.5.3。
**跑这些工具必须用该 venv 的 python**，系统 python 无 numpy。

### 实测结论（234498 采样点，步长 1°）

**真机 J1/J2/J3 全网格可达的 TCP 范围：**

| 轴 | 范围 (mm) |
|---|---|
| X | 40.686 .. 177.091 |
| Y | -153.365 .. 153.365 |
| Z | 48.965 .. 114.693 |
| R = hypot(X,Y) | 81.373 .. 177.091 |

**IK 的判据（纯几何，未叠加限位）：** `D ∈ [0,160]` ⇒ `R = D+40 ∈ [40,200]`。
⇒ **内圈差 41.4mm**（R 下限 81.4 vs 40），这部分完全由 elbow 限位吃掉。

**★ 真正的发现（contrarian，值得记住）：**

1. **`reachable_zero` 被拒 `JOINT_LIMIT` 不是实现缺陷。**
   全零位形下臂笔直 ⇒ `D = L1+L2 = 160` 恰好落在可达壳**边界**，
   `alpha=0` ⇒ `elbow_abs = shoulder = 0°`，而 elbow 限位下限是 108.44°
   ⇒ 差 108.4415°。这是**几何必然** —— 真机肘关节物理上弯不到那么直。
   ⚠️ 推论：`sim2sim.json` 里 `zero` 用例的 FK 输出（40,0,220）**不该**被当作
   "可达性基准点"，它是个**退化位形**。

2. **elbow-up 支完全正确，elbow-down 支的 `shoulder`/`elbow` 值互换了。**
   实测 home / shoulder_min / shoulder_max / elbow_min / elbow_max 五个点，
   elbow-up 支**全部精确解得回原角（越界 0.000000）**；
   elbow-down 支给出 `shoulder=112.62, elbow=0.85` —— 正是"把绝对角当相对角"的形态。
   ✅ 这是 `elbow-down` 分支的**固有多解特性**，不是 bug（2R 两支解在
   绝对角表述下天然镜像）。但它意味着：**`prefer: 'nearest'` 是必需的**，
   固定选支会在内边界附近翻肘。

3. **elbow 限位端点在整数度量化后两端都掉出限位：**

   | 关节 | 限位 | round | Δ | 结论 |
   |---|---|---|---|---|
   | shoulder | -6.093683 | -6 | +0.093683 | ✓ 仍合法 |
   | shoulder | 49.454929 | 49 | -0.454929 | ✓ 仍合法 |
   | elbow | 108.441485 | 108 | -0.441485 | ✗ 越界 0.441485° |
   | elbow | 141.858221 | 142 | +0.141779 | ✗ 越界 0.141779° |

   **安全整数区间**：shoulder `[-6, 49]`、elbow `[109, 141]`。
   ✅ 印证铁律「限位端点不可精确到达」——**elbow 两端都踩**，不只是端点。

   舵机换算：elbow `scale=2.39401` ⇒ 越界 0.441485° ≈ 舵机 1.057°。

### 未决 / 待确认

- 上述是**模型层**的差距。用户最初提到"lk 反解的 limit 参数"，真正想问的可能是
  **真机（固件 `ERR JOINT`）侧**的限位是否与该模型一致 —— 需确认后再动。
- `ws_scan` 的 `R=0` 段在 1° 网格下无采样点（base 为整数度时 X 不可能为 0），
  已改用 `R<2mm` 口径；如需竖直轴精确剖面需单独扫。

---

## 网页端「边界值」诊断收口 + 边界计算方式文档

### 用户报告的复现现象

网页拖滑杆到极限 → 记 XYZ → 回 HOME → 手输该 XYZ → 报 `JOINT_LIMIT@shoulder/elbow`。
截图：J1=29.5 / J2=49.1 / J3=141.8，Target = `(130.4, 73.8, 49.5)`，报
`JOINT elbow 141.862303 (limit 108.4414852068..141.8582211436)`。

### 根因（两层，互相独立）

1. **界面 1 位小数舍入 vs 判据 1e-9 容差**
   - FK 精确 TCP = `(130.5020, 73.8345, 49.5107)`，用户手输 `(130.4, 73.8, 49.5)`
   - 输入差 **0.108mm**；解出 `elbow = 141.862303°`，超上限 `141.8582211436°` **仅 0.0038°**
   - ⇒ 误判量级差 **八个数量级**，不是精度问题
2. **球壳判据不含限位信息**
   - `d = hypot(r−40, z−60)`，可达 ⟺ `d ∈ [0, 160]`
   - 截图点 `d = 110.336` **深在壳内** ⇒ 层① 必然放行，问题全暴露在层②

⚠️ **反直觉确认**：把 FK 精确值喂回 IK，偏差 **0.0000%** ⇒
**web 端计算链无 bug**（FK/IK/标定/限位自洽）。问题纯在"显示精度 ↔ 判据容差"错配。

⚠️ 用户报的另一组 `(97, 157, 49)`：`r = 184.548mm` > 真机扫描上限 `177.091mm`
（超 7.457mm）⇒ **该点物理不可达**，与实现无关。

### 代码修改：全部放弃

用户先要求「只用球面壳判据 + 在"末端目标选项"支持调整范围」，我实施了
`targetGuard.ts`（probe-based 二分内缩）+ 全量钳制 ⇒ **13 个测试失败**
（`drag-tracking.test.ts` 9 条 + `robotStore.target.test.ts` 4 条）——
因为钳制与 **Phase 6 冻结语义**（"越界即拒绝、关节逐位不变"）冲突。
随后用户明确「**放弃刚才的修改**」⇒ 已全部 `git checkout` / `rm` 还原，工作区干净。

★ **教训**：Phase 6 的"越界即拒绝"是有测试守护的**冻结语义**，改它必须走
D80 连锁清单，不能顺手改。

### 交付：`docs/workspace-boundary.md`

用户最终只要**文档**，不要改代码。文档覆盖两条路径（**只描述现状**）：

| 路径 | 有无钳制 | 边界算法 |
|---|---|---|
| **XYZ 数字输入** | ❌ 输入框无 `min`/`max`，无任何预判 | 层① 球壳 `d∈[0,160]` → `OUT_OF_WORKSPACE`；层② base/shoulder/elbow 限位 `violation ≤ 1e-9` → `JOINT_LIMIT @ joint` |
| **鼠标拖动** | ❌ **没有任何范围计算** | 射线 ∩ 冻结平面 → 交点**原样** `moveTo()`；`dragTarget()` 返 null 时**保持**上一目标（非钳到边界） |

**拖动路径的关键事实（易误解）**：
- `pointerdown`：`resetTarget()` 吸附到真实 TCP + `makePlane()` 冻结平面
  （冻结必要：每帧重建会形成"目标↔TCP↔平面"自反馈环，手势飘走）
- 唯一的边界表现是**事后**：`useFrame` 读 `ikStatus?.ok` 决定把手球颜色
  （`COLOR_OUT_OF_REACH = '#e5534b'`），分离 > `SEPARATION_EPS_MM = 0.4mm` 画虚线
- ⇒ **把手会一路跟着鼠标跑出工作空间再变红**，无"软墙"手感（设计意图，非 bug）
- ⚠️ 默认 `xy` 模式**锁死 Z**（`snapToMode` 精确取回 `anchor[2]`）⇒ 锚点 Z 贴边时
  整条拖动注定越界（`dz` 变常量，球壳退化成水平圆环）

### 校验过的数字（勿凭记忆改）

- `reach = [0, 160]`（`|l1−l2|, l1+l2`，l1=l2=80）；`toolOffset = [40,0]`；`pivotZ = 60`
- 真机 R ∈ `[81.373, 177.091]` vs 几何 `[40, 200]` ⇒ 内圈差 41.373 / 外圈差 22.909
- `ik_limit_gap_pct.py` 实测输出：**16 项判定 · 正常 14 · 超 5% 的 2**
  （那 2 项是**我自己分母选错**——拿 `R_max=200` 去比 `R_min` 的差值，非模型缺陷）
- 自反性 5 组姿态（home/shoulder_min/shoulder_max/elbow_min/elbow_max）全部 **0.0000%**

---

## 末端目标「安全参数」落地（三条约束实现）

### 用户需求（逐字）

> 1. 硬误差保证在2%以内(限位可调)
> 2. 几何球壳的内径可调(选项在末端目标选择中支持修改，最小为0.1，其它方式不能变化)
> 3. 不影响任何算法，仅影响参数

决策点经确认：**只许向内收紧** / **选项 C（只加前置检查）** / **容差默认 0**。

### ★ 核心认知修正（最重要，勿忘）

**「限位可调」根本解决不了「手输被拒」。**

- `elbow` 限位 `[108.4414852068, 141.8582211436]` **恰好等于** `servo_8` 硬限位
  `20..100` 经标定（`reverse=true, scale=2.39401, offset=359.61`）反算的值
  ⇒ **放宽余量 = 0.000000°**。`shoulder` 同理（`servo_7` `80..160` → `[-6.093683, 49.454929]`）。
- ⇒ 限位**一个度都放不宽**（放宽 ⇒ `ACTUATOR_REACH` 校验失败）。
- 反过来，**收紧**只是把悬崖搬个位置 —— 新极限点回输仍被 `EPS_DEG = 1e-9` 拒。
- ⇒ **真正起作用的是「判据容差 T」**；「限位可调」只是可选的保守化手段。

### 2% 反推 T

最坏力臂 `L = l1 + l2 = 160`，钳位误差 `≈ L·sin(T)`，基准 `MEASURED_MAX_RADIUS_MM = 177.091`：

| T | 最坏位移 | % | 判定 |
|---|---|---|---|
| 0.5° | 1.396mm | 0.789% | ✅ |
| **1.0°** | 2.793mm | **1.577%** | ✅ ← 取为上限 |
| 2.0° | 5.585mm | 3.154% | ❌ |

### 落地文件

| 文件 | 性质 |
|---|---|
| `frontend/src/robot/model/parameterOverrides.ts` | **新增**：`TargetGuardParams` / `REACH_MIN_FLOOR_MM=0.1` / `JOINT_TOLERANCE_MAX_DEG=1.0` / `MEASURED_MAX_RADIUS_MM=177.091` / `normalizeTargetGuardParams` / `modelWithTightening`（带缓存） |
| `frontend/src/store/robotStore.ts` | `targetGuard` 状态 + setter + `moveTo` 两处前置检查 |
| `frontend/src/components/RobotControl/TargetControl.tsx` | 可折叠「安全参数」面板 |
| `frontend/src/styles.css` | `.guard-*` 样式（无装饰色） |
| `frontend/tests/unit/targetGuardParams.test.ts` | **新增 22 条断言** |
| `robot-package/**` | **零改动**（`git diff` 为空 = 约束③ 机器证明） |

`moveTo` 注入点：
1. **参数层① 内径覆写**：`solveIk` **之前**独立检查 `wristDistance < reachMinMm − 1e-9`
   ⇒ 直接返 `OUT_OF_WORKSPACE`。因 `reachMinMm ≥ 0.1 > 原 reachMin = 0`
   ⇒ **恒比原判据更严**，原判据被完全包含，**外径精确不动**；
   且 `solveIk` 被直接调用的地方（sim2sim bridge）看不到 ⇒ 天然隔离。
2. **参数层② 容差**：`T > 0` 时才进，`pickWithinTolerance(candidates, T)`。

★ 关键技巧：`ik.ts` 的 `geometryCache = WeakMap<RobotModel, …>` ⇒ 传**新模型对象**
即天然缓存失效，**无需改 ik.ts**；但必须**缓存住**覆写后的模型，否则拖动每帧 miss。

### 验收（2026-09-16 实测，全绿）

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| `vitest run` | **33 文件 · 423 passed**（401 基线 + 22 新增） |
| `pytest -q` | **198 passed**（基线不变） |
| `tests/sim/test_baseline_frozen.py` | **11 passed** |
| `tests/sim2sim` | **9 passed** |
| `core/tests` | **31 passed** |
| `run_sim2sim.py` | FK ≤7.110e-14mm · IK 25/28 · 闭环 ≤1.025e-13mm，与基线一致 |
| `drag-tracking.test.ts` / `robotStore.target.test.ts` | **11 + 13 passed**（Phase 6 冻结语义） |
| `corePackageBoundary.test.ts` | 5 passed（**未扩边界**） |
| `vite build` / `go test ./...` | 成功 / 全绿 |
| `git diff robot-package/` | **空** |

### 实施中修掉的两个真 bug

1. **`worstClampPercent` 公式自相消**（UI 显示用）：原实现
   `(d/reachMax)*100*(reachMax/177.091)` 把 `reachMax` 既当力臂又当分母
   ⇒ 对外径 50 的机器人给出 0.49%（正确值仍是 1.577%）。
   修为 `(momentArm*sin(T) / MEASURED_MAX_RADIUS_MM)*100`，
   并加断言「界面基准常量 == 验收常量」防漂移。
2. **测试构造点不保证带舍入误差**：`atElbowMax()`（`base=0, shoulder=0` + elbow 上限）
   按 1 位小数舍入后**可能不带误差**（恰好落在 0.1mm 整格）⇒ 前提不成立。
   改为**穷举实际可达极限姿态**、取"精确贴限位但舍入后回输被拒"的点。

⚠️ **测试不能 `import { solveIk } from '@robot'`** —— `ik.ts` 刻意不从桶出口再导出
（Core 里不得出现型号名）。探针须走 `store().moveTo()`。

---

## 末端目标「安全参数」改版：删限位调节 + 加球壳外径覆写

### 用户需求（逐字）

> 去除安全参数中的所有限位调节(同时删除后端相关可调参数），增加球壳外径覆写，
> 支持从1~160mm，启用修改后是实时变化并参与计算

### ★ 三个决定性认知（本轮实测，勿忘）

**① 外径覆写必须开在 `ik.ts` 里面。**
需求"参与计算"的落点是 2R 的 `cosAlpha`（`d² = l1²+l2²−2l1l2cos(π−θ)`）。
`if (d > reachMax)` 只决定"能不能解"，`cosAlpha` 才决定"解出来是多少"。
在 `ik.ts` 外面拦一道**只能实现"拒绝更早"**，不能实现"按新臂展求解"。
⇒ 本轮对 `ik.ts` 做**最小 opt-in**：`IkOptions.reachMaxMm?: number`。
`undefined` ⇒ 用求导外径，**语义一字节未变**（实测 `抽象层残差差 0.000e+0`）。

**② 外径覆写的有效区间远小于 UI 的 1~160。**

| 量 | 值 |
|---|---|
| 几何球壳 `ikGeometry().reach` | `[1.42e-14, 160]` |
| **合法限位域内实际腕枢轴距离 `d`** | **`[44.1665, 139.2662]`** |
| 最大点 | `shoulder=max, elbow=min` → `139.2662`（两杆近乎伸直） |
| 最小点 | `shoulder=min, elbow=max` → `44.1665`（折得最狠） |

⇒ 填 150/160 **等于没覆写**。UI 把 `max` 显示为 `min(160, 求导外径)`。

**③ 「限位可调」整段移除是有实测依据的**（不是嫌麻烦）：
关节限位映射到舵机后必须落在舵机硬限位内（`ACTUATOR_REACH`），
本机余量 **0.000000°** ⇒ 限位**一个度都放不宽**，"调节"只剩向内收紧，
而向内收紧已被**球壳内径覆写**更直接覆盖（1 个标量 vs 逐关节 2 端点）。
⇒ 删 `jointTightenDeg` / `tightenLimits()` / `applyJointTightening()` /
`modelWithTightening()` / `overrideCache` 全套。

### ★★ 两个测试构造坑（都导致过假性通过/失败）

1. **"外圈点"不能想当然。** 初版用 `reachable({shoulder:0, elbow:141})` 当外圈点，
   但它 `d` 只有 **19.5mm**（`shoulder=0` 让手臂折回）⇒ 用它验证"外径收紧拦住外圈"
   会假性通过。正解：`atOuterShell()` = `shoulder=max, elbow=min`（`d=139.2662`）。
2. **求 `radial` 不能用 `wrist.parentLink` 的长度。**
   `tcp.joint='tool'` 的 `parentLink` 是 **`forearm_link`(80)** 而非 `tool_link`(40)
   ⇒ 算出的 `d` 偏小（94.15 vs 真值 131.96），断言假性通过。
   正确口径：`radial = geometry.toolOffset[0] = model.tcp.offset` 的径向分量（= 40）。
   已在 helper 里加 `expect(pivotZ).toBeCloseTo(60)` / `expect(radial).toBeCloseTo(40)` 自校验。

### 落地文件

| 文件 | 性质 |
|---|---|
| `robot-package/mearm-v1/kinematics/ik.ts` | **opt-in 改**（47+/5−）：`IkOptions.reachMaxMm?` + `solveIkCandidates` 第三参 + 报错文案带覆写上下文 |
| `frontend/src/robot/model/parameterOverrides.ts` | **重写**：删限位全套；加 `reachMaxOverridden` / `reachMaxMm` / `REACH_MAX_FLOOR_MM=1` / `REACH_MAX_CEILING_MM=160` / `reachOverrideChangesSolve` / `reachMaxOverrideFor` |
| `frontend/src/store/robotStore.ts` | `moveTo` 三处（内径/外径前置检查共用一次 `wristDistance`）+ 透传 `reachMaxMm` + **`setTargetGuard` 内主动重解当前 target**（实现"实时"） |
| `frontend/src/components/RobotControl/TargetControl.tsx` | 第三段 UI：关节限位 → **球壳外径覆写**；删 `adjustableJoints()`；内径 `max` 随生效外径联动 |
| `frontend/tests/unit/targetGuardParams.test.ts` | **重写 32 条**（需求 ①②③④） |
| `docs/target-guard-analysis.md` | §7.1/7.2/7.3 更新 + 新增 §7.6（为何无限位调节）/§7.7（为何必须进 ik.ts）/§7.8（有效区间） |
| `backend/` · `config/` | **未动**：已核实不存在可调限位参数（`cfg.go` 明写"运行配置里绝不允许出现关节限位或标定数值"；`grep` 只命中 `protocol.CodeJointLimit` 错误码常量） |

### 验收（2026-09-16 实测，全绿）

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | **0 错误** |
| `vitest run` | **33 文件 · 433 passed** |
| `pytest -q` | **198 passed**（基线不变） |
| `tests/sim/test_baseline_frozen.py` | **11 passed** |
| `tests/sim2sim` | **13 passed** |
| `run_sim2sim.py` | FK ≤7.110e-14mm · IK 25/28 · 闭环 ≤1.025e-13mm，**与基线一致** |
| `ik.test.ts` | **24 passed**（ik.ts 改动未破坏） |
| `drag-tracking` / `robotStore.target` / `corePackageBoundary` | 11 + 13 + 5 passed |
| `vite build` / `go test ./...` | 成功 / 全绿 |
| `git diff robot-package/` | 仅 `ik.ts`，`robot.yaml` **零改动** |
| `grep -r tighten frontend/src` | **零命中** |

### 修掉的 2 条失败断言（旧常量残留）
`reachMaxMm:100` 配 `shoulder.max*0.8`（`d=131.957`）⇒ 覆写值根本拦不住。
改法：采点改 `shoulder.max*0.5`（`d≈112`，落在 `(44.1665, 139.2662)` 内），
覆写值改用 **130**（> d ⇒ 不干涉）；另加一条"同一点 130 可达 / 100 被拒"
把"边界随参数移动"钉死。锚点断言标题 `[16.654, 100.302]` → `[44.1665, 139.2662]`。

---

## ★★ 事故：store 的 `d` 与 `ik.ts` 的 `d` 不是一个量（用户报"改小外径反而越界"）

### 用户报告

> 为什么本身可运行范围，我把外径覆写从160改到60，反而OUT_OF_SPACE了

### 根因：`wristDistance()` 把 `radial` 取错了连杆

`robotStore.ts` 的前置检查自己重算 `d`，其中

```ts
const radial = linkLengthOf(model, wrist.parentLink);   // ❌ wrist = model.tcp.joint = 'tool'
```

`tcp.joint='tool'` 的 `parentLink` 是 **`forearm_link`（80）**，不是 `tool_link`（40）。
而 `ik.ts` 的口径是 `toolOffset[0] = 40`（`requireConstantToolOffset()` 实测）。

同一目标点 ⇒ **两个 `d`**：

| 来源 | `radial` | `d`（同一点） |
|---|---|---|
| `ik.ts`（正确） | `toolOffset[0]` = 40 | **111.542** |
| `wristDistance()`（错） | `forearm_link` = 80 | **92.159** |

差 **19.38mm**，且**不报错**。函数注释还写着"`radial` = `tool_link.length`（本机 40）" ——
**注释与代码不一致**，注释描述的是意图，代码读的是另一个连杆。

### 症状为什么迷惑人

- 错误 `d` **系统性偏小**（`radial` 越大 ⇒ `|r − radial|` 越小），偏 19~40mm
- 后果 ①：**内圈一大片实际不可达的点被显示成可达** ⇒ 用户以为"可运行范围"更大
- 后果 ②：把外径降到 60 时，真实 `d=111.5` 当然超 60；而那个点在**外径 160 下本来就失败**
  （只是原因不同：`JOINT_LIMIT @ elbow` 而非 `OUT_OF_WORKSPACE`）
  ⇒ 所以**报错是"碰巧对"的**：判定结果对，依据的数是错的
- 后果 ③（更危险）：口径不一致 ⇒ 两层的拒绝理由随机（有时这层、有时 `ik.ts` 那层）

### 修复：删掉自研几何，一律转发 `ik.ts`

```ts
function wristDistance(model, target): number {
  const geometry = ikGeometry(model);
  const { dr, dz } = wristSagittal(geometry, target);
  return Math.hypot(dr, dz);
}
function reachShellOf(model): readonly [number, number] {
  return ikGeometry(model).reach;   // 也是转发，理由同上
}
```

- `reachShellOf()` 原本自己按 `[|l1−l2|, l1+l2]` 从连杆表读 —— 本机数值恰好一致，
  但**不是同一份真值**（`ikGeometry()` 是从 FK 采样反推）。一并改为转发。
- 副作用：`robotStore.ts` 的 import 多取 `ikGeometry` / `wristSagittal`
  ⇒ **已登记进 `corePackageBoundary.test.ts` 的 ALLOWED 白名单**（双向断言：多取/少取都失败）。
- `dWrist` 类型 `number | null` → `number`，调用点改判 `reachOverridden`。

### ★ 教训（写进铁律）

**「同一个量只能有一套口径」不是洁癖，是这条规则真的会以"轻微、随机、不报错"的方式咬人。**
凡是"为了不扩依赖边而自己重算一遍真值"的写法，都是这个 bug 的温床 ——
`ik.ts` 里恰好有一个 `requireConstantToolOffset()` 专门用三个姿态交叉验证这个偏移常量，
**说明这个量本来就容易算错**，更不该在第二处重算。

⚠️ 注意我在**上一轮就已经在测试 helper 里踩过同一个坑并修掉了**
（`targetGuardParams.test.ts` 的 `wristDistanceOf`），但**没有回头查生产代码**。
⇒ 教训 2：**在测试里发现的口径坑，必须立刻回头 grep 生产代码有没有同款。**

### 新增回归断言

`targetGuardParams.test.ts` 加 `★★ 前置检查的 d 必须与 ik.ts 同口径`：
用极小外径（= `REACH_MAX_FLOOR_MM`）强制前置检查触发，从错误消息抠出 store 算的 `d`，
与独立复算比对，**15 个采样点**全覆盖（`shoulder` 5 档 × `elbow` 3 档）。

### 本轮验收（全绿）

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | **0 错误** |
| `vitest run` | **33 文件 · 434 passed**（+1 回归） |
| `pytest -q` | **198 passed** |
| `test_baseline_frozen` | **11 passed** |
| `run_sim2sim.py` | FK ≤7.110e-14 · IK 25/28 · 闭环 ≤1.025e-13，**与基线一致** |
| `vite build` / `go test ./...` | 成功 / 全绿 |
| `corePackageBoundary.test.ts` | **5 passed**（白名单已登记 2 个新符号） |




---

## 二、skill 全库归一化（31 个） + git commit（09:08 起）

### git commit 落地

`ffd4ed6` ✨ feat(web): 末端目标安全参数改版（删限位调节/加球壳外径覆写）+ 修 d 口径不一致
—— **19 files · 3960 insertions · 23 deletions**，提交后 `git status --porcelain` 为空。
提交前做了敏感信息正则扫描（`password|secret|token|api_key|PRIVATE`）**零命中**；
`core/tools/out/`（网格扫描产物，可重算）已取消暂存并补进 `.gitignore`。

### skill 精简：从「13 个超限」到「全库合规」

原状：31 个 skill 里 **13 个正文超 11k 字符**，最大 `soc-debug-verification` 28393。
做法：把细节按节拆进 `references/*.md`，正文只留核心 + 指针表；description 只留「路由判据」。

| skill | 正文 前 → 后 | 拆出的 refs |
|---|---|---|
| `soc-debug-verification` | 28393 → 10624 | `bat-pitfalls.md`、`sandbox-exe-pitfalls.md` |
| `stm32-project-scaffold` | 27557 → 8288 | `cmake-cross-build`、`linker-script`、`oneclick-scripts`、`readme-convention`、`vscode-multi-project`、`sys-startup`、`lvgl-multi-page-ui` |
| `soc-camera-rtsp-agent` | 16624 → 8210 | `gstreamer-pipeline-pitfalls`、`rk3568-port-checklist`、`video-server-integration` |
| `soc-edge-ai-zerodep` | 14603 → 9572 | `phase2-metadata-report.md` |
| `soc-video-streaming` | 12341 → 5890 | `hls-black-screen-bug`、`hls-latency-tuning`、`e2e-verify-gate` |
| `stm32-peripheral-drivers` | 14660 → 10561 | `lan8720a-rmii`、`uart-physical-bridge` |
| `stm32-verification-acceptance` | 12053 → 5848 | `gdb-function-level-debug.md` |

**description 精简 10 个**（超 500 的）：`soc-debug-verification` 1150→202、
`stm32-project-scaffold` 863→292、`stm32-cmsis-dap-probe` 573→324、
`stm32-keil-port` 665→383、`stm32-logging-print-log` 654→353、
`stm32-peripheral-drivers` 668→256、`schematic-pdf-netlist` 530→403、
`zephyr-stm32-porting` 541→194、`soc-windows-gstreamer-build` 509→221、
`stm32-verification-acceptance` 509→458。

### ★★ 本轮抓到的两个「校验工具自己会骗人」的实例

**实例 1：正则量 description 会漏算折叠式。**
我最初用 `re.search(r'^description://s*(.*)/$')` 取**首行**，于是 4 个用 YAML 折叠式
（`description: >`）的 skill 被量成 **「1 字符」**，总量报成 **8752**。
改用真 `yaml.safe_load` 后真相是 **10145 字符 / 每条会话**（差 **1377**）。
⇒ 与铁律「自研解析器禁止用于复杂标准格式」同源：**量 YAML 就用 YAML 解析器。**

**实例 2：折叠式 description 里藏了一颗 YAML 定时炸弹。**
把这 4 个归一化成单行时，`webgl-first-frame-forensics` 直接 **YAML 解析失败**：
`mapping values are not allowed here` —— 它的正文里有裸 `Triggers: ...`，
**plain scalar 里的 `: `（冒号+空格）不合法**。它「一直能用」只是因为折叠块容忍度更高；
一旦有人把它改写成单行就会炸。⇒ 现全部加引号包裹。

### ★ 校验缺口：孤儿 references 查不出来

「围栏守恒 + 零行丢失」两项**只验内容没丢**，**验不出「拆出来的文件没人引用」**。
实测 22 个 refs 里 **14 个曾是孤儿**（文件存在、正文零引用 = 永远不会被加载）。
修法：给 8 个 skill 补「本 skill 的参考文件」指针表（`stm32-project-scaffold` 7 个、
`soc-camera-rtsp-agent` 3 个、`soc-video-streaming` 3 个、`stm32-peripheral-drivers` 2 个、
`robotics-multiphysics-vmodel-workflow` 2 个，其余各 1 个）。
⇒ 审计脚本扩成**三项**：围栏守恒 + 零行丢失 + **引用双向（无死链、无孤儿）**。

### 最终审计（全库 31 个）

```
skill 数: 31
description 合计: 10145 字符/每条会话（全部单行、全部 ≤500，最大 468）
正文合计: 202666 字符（全部 < 11000）
references 文件合计: 22（全部被引用，零孤儿）
超 11000 的正文: 无
超 500 的 desc : 无
YAML 解析（yaml.safe_load 全 31 个）: 问题 无
问题: 无
```

拆分纪律双校验（对留有原文备份的两个大件）：
`soc-camera-rtsp-agent` 围栏 24==24、非标题行 247 缺失 0；
`stm32-project-scaffold` 围栏 40==40、非标题行 401 缺失 0。

### 关于「并同步」的落实

★ `~/.workbuddy/skills/` **不在任何 git 仓库内**（`~/.workbuddy` 不是 git repo），
所以「同步」不可能指 commit。落实为三件：
① 把可复用约定写进 `MEMORY.md` §四（单行 description 铁律 + 必须用 YAML 解析器计量 + 必须补 refs 索引）；
② 本日志记录明细；
③ 清理全部分叉状态：删除 `.workbuddy_bak_*` 两个备份、`.workbuddy/_skill_bak/`、
项目内 15 个一次性 Python 脚本。**仓库内不留任何临时/备份文件。**

### 残留

无。全库合规；仓库工作区干净（除本日志与 `MEMORY.md` 的改动待提交）。
